// 1. Exporta una función.

export function upperGreet(name) {
    return(name.toUpperCase())
}

console.log(upperGreet("hector"))

// 2. Exporta una constante.

export const languaje = "es"

// 3. Exporta una clase.

export class Property {
    constructor (type) {
        this.type = type
    }

    house(){
        return type = "Inmuble familiar de máximo tres pisos"}

    building(){ 
        return type = "Inmueble de mas de tres pisos"}

    store() {
        return type = "Inmueble para almacenamiento"}

}

// 4. Importa una función.

// 5. Importa una constante.

// 6. Importa una clase.

// 7. Exporta una función, una constante y una clase por defecto (en caso de que lo permita).

// 8. Importa una función, una constante y una clase por defecto (en caso de que lo permita).

// 9. Exporta una función, una constante y una clase desde una carpeta.

// 10. Importa una función, una constante y una clase desde un directorio diferente al anterior.